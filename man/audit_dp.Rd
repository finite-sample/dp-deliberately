% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{audit_dp}
\alias{audit_dp}
\title{Run all supported audit modules}
\usage{
audit_dp(x, contrasts = list(), policy = NULL, config = audit_config())
}
\arguments{
\item{x}{A \code{\link[=deliberation_data]{deliberation_data()}} bundle.}

\item{contrasts}{A contrast or list of definitions from \code{\link[=define_contrast]{define_contrast()}}.}

\item{policy}{Optional versioned \code{\link[=audit_policy]{audit_policy()}}. NULL produces no verdicts.}

\item{config}{Configuration from \code{\link[=audit_config]{audit_config()}}.}
}
\value{
A \code{deliberation_audit} with metrics, capabilities, validation,
policy_results, evaluated contrasts, evidence, and reproducibility metadata.
}
\description{
Run all supported audit modules
}
\examples{
result <- audit_dp(example_deliberation(), config = audit_config(permutations = 9))
head(result$metrics)
}
